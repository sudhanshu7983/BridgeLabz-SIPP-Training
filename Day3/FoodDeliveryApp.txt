START
CLASS Customer
    ATTRIBUTES: cName, address, phone
     
    CONSTRUCTOR(name,address,phone)
     SET this.name=name
     SET this.address=adDress
     SET this.phone=phone

    METHOD getDetails():
	PRINT name+" "+address+" "+phone

CLASS Restaurant EXTENDS CUSTOMER
    ATTRIBUTES: rName, location, menu
	ATTRIBUTE Customer customer

    CONSTRUCTOR(rName,location,menu,customer)
	SET this.rName=rName
	SET this.location = location
	SET this.menu = menu
	SET this.customer = customer
    METHOD getMenu():
	PRINT rName"+" "+ location +" "+ menu +" "+ customer

CLASS DeliveryBoy
    ATTRIBUTES: name, id, phone
	ATTRIBUTES Restaurant restaurant
    
    CONSTRUCTOR(name, id, phone, restaurant)
	SET this.name = name
	SET this.id = id
	SET this.phone = phone
	SET this.restaurant = restaurant
    METHOD getDetails():
	PRINT name+" "+id+" "+phone+" "+restaurant

CLASS Order
    ATTRIBUTES: customer (Customer), restaurant (Restaurant), deliveryBoy (DeliveryBoy), items, totalPrice
    CONSTRUCTOR(customer, restaurant, deliveryBoy, items, totalPrice)
	SET this.customer = customer
	SET this.restaurant = restaurant
	SET this.deliverBoy = deliveryBoy
	SET this.items = items
	SET this.totalPrice = totalPrice
    METHOD placeOrder():
	PRINT customer+" "+ restaurant+" "+ items +" "+ totalPrice
    METHOD showOrderDetails()
	PPRINT customer+" "+ restaurant+" "+ deliveryBoy+" "+ items +" "+ totalPrice

MAIN
    CREATE customer = new Customer(name, address, phone)
    CREATE restaurant = new Restaurant(name, location, menu, customer)
    CREATE deliveryBoy = new DeliveryBoy(name, id, phone, restaurant)

    CREATE order = new Order(customer, restaurant, deliveryBoy, items, totalPrice)

    CALL order.placeOrder()
    CALL order.showOrderDetails()

END